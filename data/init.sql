CREATE DATABASE IF NOT EXISTS nodedb;

CREATE USER IF NOT EXISTS 'root' IDENTIFIED BY 'root';
GRANT ALL ON nodedb.* TO 'root';

CREATE TABLE IF NOT EXISTS `people` (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255)
);